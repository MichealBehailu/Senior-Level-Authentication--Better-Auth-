import { createAuthClient } from "better-auth/react"
export const authClient = createAuthClient()

//this is used as  Frontend remote control 
//no need to add providers here already done the auth.ts in there it is already done 